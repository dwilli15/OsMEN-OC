# osmen-media.network
# Bridge network for media services (Plex, *arr stack, Kometa, Tautulli).
# Download stack uses its own pod with shared gluetun namespace instead.

[Network]
NetworkName=osmen-media
Driver=bridge
Subnet=10.89.1.0/24
Gateway=10.89.1.1
IPv6=false
