# Active Context

Current branch: install/fresh-setup-20260407.
Immediate work: Google OAuth minimal Calendar auth is now proven via `gog` with a manual localhost callback; next steps are local SOPS backup creation, runtime env wiring, plaintext credential cleanup, and dirty-tree provenance triage before resuming broader install work.
Known correction: VPN secrets must match gluetun quadlet names (`osmen-vpn-wireguard-private-key`, `osmen-vpn-wireguard-addresses`).
