# Tech Context

Ubuntu 26.04, Python 3.14 venv in `/home/dwill/dev/.venv`, rootless Podman 5.7, systemd user Quadlets.
Local secret backups live at `~/.config/osmen/secrets/`; the age key lives at `~/.config/sops/age/keys.txt`.
Repo `config/secrets/` is template-only.
