# 2026-04-14 — Daily Notes

## Plex: Container → Native Install
- Plex container (`osmen-media-plex`) had broken claim token (`target=PLEX_CLAIM` quadlet syntax wasn't being respected by Podman 5.7.0 — env var showed as `osmen-plex-claim` instead)
- Plex first-ran with bad claim, leaving it in half-claimed "not authorized" state
- D decided to ditch containerized Plex entirely — native `.deb` install instead
- **Actions taken:**
  - Stopped and removed container, image (`pms-docker`), volume (`systemd-osmen-plex-config`), secret (`osmen-plex-claim`)
  - Removed deployed quadlet from `~/.config/containers/systemd/`
  - Installed `plexmediaserver` via Plex apt repo
  - Claimed server via API with token `claim-5apKZFVy_xEmyzUaJX8W`
  - Server running as `plexmediaserver.service` (system-level systemd)
- **Code updates:**
  - `core/gateway/builtin_handlers.py` — `assess_plex_readiness` check 5: podman inspect → `systemctl is-active plexmediaserver`
  - `config/secrets-registry.yaml` — removed `plex_claim_token` entry
  - `tests/test_handlers.py` — updated `plex_container_running` → `plex_service_running`
  - `.github/copilot-instructions.md` — noted Plex exception to container naming
  - `quadlets/media/osmen-media-plex.container` — marked DEPRECATED

## Webchat Model Dropdown
- Only 4 models were showing — `agents.defaults.models` was the allowlist
- Added all 19 models (13 zai + 4 ollama + 2 github-copilot) via config.patch
- Working after hard refresh

## Plex Account
- Email: d.osmen.oc@gmail.com (from claim response)
